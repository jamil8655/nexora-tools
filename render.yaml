services:
  - type: web
    name: nexora-media-server
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server/video-server.js
    envVars:
      - key: NODE_ENV
        value: production
