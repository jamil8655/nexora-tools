services:
  - type: web
    name: nexora-tools-vgti
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server/video-server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
